import React from "react";
import "../css/ApplyJob.css"

const ApplyJob = (props) => {
    return (
        <div className="">
            {props.apply}
        </div>
    );
};

export default ApplyJob;